<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/base"
        android:layout_marginTop="1dp"
        android:layout_marginEnd="@dimen/base"
        android:layout_marginBottom="1dp">

    <TextView
            android:id="@+id/songNumberTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/base"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="#2"/>

    <TextView
            android:id="@+id/songNameTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/base"
            android:layout_marginTop="@dimen/base_small"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/youtubeImageView"
            app:layout_constraintStart_toEndOf="@id/songNumberTextView"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Song name"/>

    <TextView
            android:id="@+id/songArtistTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/base"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="@dimen/base_small"
            android:ellipsize="end"
            android:maxLines="1"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/youtubeImageView"
            app:layout_constraintStart_toEndOf="@id/songNumberTextView"
            tools:text="Song artist"/>
    <ImageView
            android:id="@+id/youtubeImageView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:src="@drawable/youtube_ic"
            android:contentDescription="@null"
            android:layout_margin="@dimen/base"
            android:layout_marginEnd="8dp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/songTimeTextView"
            tools:text="03.36"/>
    <TextView
            android:id="@+id/songTimeTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/base"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="03.36"/>
</androidx.constraintlayout.widget.ConstraintLayout>